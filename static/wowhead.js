// Wowhead tooltip widget config (loaded before power.js). Kept in a static file
// so the strict script-src can stay 'self' for our own code. We keep our own
// link colours/icons/names and only want the hover tooltips.
const whTooltips = {
  colorLinks: false,
  iconizeLinks: false,
  renameLinks: false,
};
