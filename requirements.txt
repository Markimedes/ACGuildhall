Flask>=3.0
# Imported directly (app.py: Markup, escape); ships with Flask but pinned here
# since we use it on its own.
markupsafe>=2.1
mysql-connector-python>=8.3
gunicorn>=21.2
# Standard-library replacements for the hand-rolled CSRF / auth / rate-limit
# plumbing (Phase 2). Kept in sync with pyproject.toml until the Phase 4 deploy
# cutover retires requirements.txt.
flask-wtf>=1.2
flask-login>=0.6
flask-limiter>=3.5
# AI news desk (Gemini). Optional at runtime -- without GUILDHALL_GEMINI_API_KEY
# the news desk stays dark -- but installed so the feature is available.
google-genai>=1.0
